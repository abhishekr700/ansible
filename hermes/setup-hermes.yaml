# Main setup playbook for the Hermes machine.
# This orchestrates core system configuration and imports project-specific playbooks.
---
- name: Setup hermes
  hosts: hermes
  vars_files:
    - vars.yaml
  # roles:
  # - roles/debian-install-docker
  # - nodejs-setup
  tasks:
    - name: Install Loki Docker Driver
      community.docker.docker_plugin:
        plugin_name: grafana/loki-docker-driver:3.6.4-arm64
        alias: loki
        state: enable

    # - name: Enable IP forwarding for Tailscale
    #   become: true
    #   ansible.posix.sysctl:
    #     name: "{{ item.key }}"
    #     value: "{{ item.value }}"
    #     sysctl_file: /etc/sysctl.d/99-tailscale.conf
    #     state: present
    #     reload: yes
    #   loop:
    #     - { key: "net.ipv4.ip_forward", value: "1" }
    #     - { key: "net.ipv6.conf.all.forwarding", value: "1" }

    # - name: Ensure 'docker' group exists
    #   ansible.builtin.group:
    #     name: docker
    #     state: present

    # - name: Add user 'abhi' to the 'docker' group
    #   become: true
    #   ansible.builtin.user:
    #     name: "{{ pi_user }}"
    #     groups: docker
    #     append: yes

    # - name: Setup Adguard
    #   ansible.builtin.include_tasks: setup-adguard.yaml

    # - name: Setup folders
    # ansible.builtin.include_tasks: setup-folders.yaml
    # - name: Setup docker networks
    # ansible.builtin.include_tasks: setup-docker-networks.yaml

    # Expense tracker setup moved to its own playbook
    - name: Setup Home Assistant
      ansible.builtin.include_tasks: setup-home-assistant.yaml

- name: Import Expense Importer Playbook
  import_playbook: setup-expense-importer-hermes.yaml
